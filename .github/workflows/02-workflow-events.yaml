name: 02 - workflow Events
on: 
  push:
  workflow_dispatch:
  pull_request:
jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
        - name: show the trigger
          run: echo "I am trigerred by an ${{github.event_name}} event."

          
          

          

